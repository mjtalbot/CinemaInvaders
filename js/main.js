require(["cinema_invaders"], function(cinema_invaders) {
    //the jquery.alpha.js and jquery.beta.js plugins have been loaded.
    cinema_invaders.initialize('some_canvas', 50);
    cinema_invaders.start();
});